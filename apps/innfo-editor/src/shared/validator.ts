/**
 * App re-export — validator logic now lives in @innv0/format-core.
 * This file preserves import paths for existing app code.
 */
import { validateFormatContent } from '@innv0/format-core'
export { validateFormatContent }
