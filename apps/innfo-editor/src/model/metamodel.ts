/**
 * App re-export — metamodel resolution now lives in @innv0/format-core.
 * This file preserves import paths for existing app code.
 */
export { resolveEffectiveMetamodel } from '@innv0/format-core'
