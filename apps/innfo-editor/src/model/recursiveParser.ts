/**
 * App re-export — recursiveParser now lives in @innv0/innfo-core.
 * This file preserves import paths for existing app code.
 */
export type { ParseIssue, RecursiveParseResult } from '@innv0/innfo-core'
export { recursiveParse } from '@innv0/innfo-core'
