{
  "name": "@innv0/format-editor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm --prefix ../../packages/format-core run build",
    "dev": "vite",
    "prebuild": "npm --prefix ../../packages/format-core run build",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "pretest": "npm --prefix ../../packages/format-core run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@innv0/format-core": "*",
    "pinia": "^2.1.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "@vue/test-utils": "^2.4.0",
    "fake-indexeddb": "^6.0.0",
    "happy-dom": "^14.0.0",
    "typescript": "^5.4.0",
    "vite": "^6.0.0",
    "vitest": "^1.6.0",
    "vue-tsc": "^2.0.0"
  }
}
